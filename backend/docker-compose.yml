version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: aiknowledgehub-mongo
    ports:
      - "27017:27017"
    volumes:
      - aiknowledgehub-mongo-data:/data/db

volumes:
  aiknowledgehub-mongo-data:
